# Incident Report — INC-2026-0142 Portfolio Simulation

> 비운영 fixture를 사람이 읽을 수 있게 정리한 예시입니다. 실제 장애, 고객 영향, root cause 또는 복구 완료 증적으로 사용하지 않습니다.

## 1. Incident Metadata

| Field | Value |
| --- | --- |
| Incident ticket | `INC-2026-0142` |
| Report status | `simulation` |
| Severity | `critical` in synthetic request |
| Environment/service | `prod / example-api` placeholder inventory |
| Analysis window | `2026-08-08T01:10:00Z - 2026-08-08T02:10:00Z` |
| Request ID | `req-01JEXAMPLE142` |
| Trace ID | `generated per simulation run; not fixed in this example` |
| Data classification | `internal synthetic` |
| Incident Commander/sign-off | `not_available — simulation` |

## 2. Executive Summary

모의 ALB target 5xx 증가와 동시에 12개 Pod 중 4개만 Ready였고 restart 17회가 관측됐습니다. 제한된 error aggregate에는 `UPSTREAM_TIMEOUT`과 deployment version `v2.18.4`가 있었습니다. 이 세 신호는 serving capacity 또는 dependency 문제를 조사할 근거지만 root cause를 확정하지 않습니다.

Customer impact, 실제 장애 지속 시간과 복구 결과는 `not_available`입니다. Agent는 외부 tool이나 변경 작업을 실행하지 않았습니다.

## 3. Timeline

| UTC | Source | Event | Evidence |
| --- | --- | --- | --- |
| `2026-08-08T01:10:00Z` | synthetic request | 분석 window 시작 | `EX-REQ-0142` |
| within window | CloudWatch fixture | ALB target 5xx 최대 84 requests/min | `ev-fixture-alb-5xx` |
| within window | Kubernetes fixture | 4/12 Ready, restart 17회, OOMKilled 2 | `ev-fixture-pods` |
| within window | Logs fixture | `UPSTREAM_TIMEOUT`, deployment `v2.18.4`, 52 errors | `ev-fixture-errors` |
| `2026-08-08T02:10:00Z` | synthetic request | 분석 window 종료 | `EX-REQ-0142` |

## 4. Confirmed Facts

| Fact | Evidence ID | Confidence |
| --- | --- | --- |
| ALB target 5xx가 baseline에서 증가해 분당 최대 84로 입력됨 | `ev-fixture-alb-5xx` | fixture 안에서는 high |
| Workload Pod 12개 중 4개 Ready, restart 17회로 입력됨 | `ev-fixture-pods` | fixture 안에서는 high |
| 제한된 log aggregate 1행이 수집되고 민감 패턴 2개가 마스킹됨 | `ev-fixture-errors` | fixture 안에서는 high |

## 5. Hypotheses and Gaps

| Hypothesis | Confidence | Support | Verify next |
| --- | --- | --- | --- |
| readiness/startup 실패가 serving capacity를 줄였을 수 있음 | medium | `ev-fixture-pods` | probe failure, release marker, Ready target 추세 비교 |
| restart가 OOM 또는 application exit와 관련될 수 있음 | medium | `ev-fixture-pods` | memory working set, limit, termination reason 상관관계 확인 |
| upstream dependency 지연이 error 증가에 기여했을 수 있음 | low | `ev-fixture-errors` | provider latency와 timeout aggregate 확인 |

Evidence gaps:

- 고객 요청량, error ratio와 실제 SLO
- 배포 시작·종료·rollback event
- Pod memory/CPU와 probe event의 시간 상관관계
- dependency latency·availability
- 복구 후 동일 window 검증

## 6. Root Cause and Impact

- Root cause: `not_confirmed`
- Customer impact: `not_available — synthetic fixture에 고객·SLI source가 없음`
- Data/security impact: `none observed in synthetic output; live audit evidence는 없음`
- Recovery state: `not_available — Agent는 복구를 실행하지 않음`

## 7. Response and Safety Boundary

Proposed operator checks:

1. deployment version별 Ready target, probe failure와 restart를 비교합니다.
2. OOMKilled 시각과 container memory limit·working set을 대조합니다.
3. upstream timeout과 dependency latency를 같은 window에서 확인합니다.
4. 실제 변경이 필요하면 별도 ticket, plan, 승인과 rollback 조건을 생성합니다.

Executed actions:

| Action | Result |
| --- | --- |
| External tool call | `0 — fixture simulation` |
| Restart/scale/rollback/failover | `none` |
| Cloud/Kubernetes mutation | `executed_mutations=[]` |

## 8. Corrective Actions

| Action | Owner | Due | Closure evidence |
| --- | --- | --- | --- |
| Schema/runtime contract alignment CI 검사 | AI Platform/CI-CD | `2026-08-31` | automated test |
| dev pilot에서 metric·event time correlation 검증 | Monitoring/Service owner | `not_scheduled` | approved pilot report |
| customer-impact SLI와 recovery validation 항목 연결 | Service/Operations | `not_scheduled` | SLO definition + post-check |

## 9. Evidence and Sign-off

| Evidence ID | Source | Handling |
| --- | --- | --- |
| `EX-REQ-0142` | `examples/incidents/prod-api-5xx-request.json` | synthetic, Git revision |
| `ev-fixture-alb-5xx` | CloudWatch-shaped fixture | synthetic aggregate |
| `ev-fixture-pods` | Kubernetes-shaped fixture | synthetic selected fields |
| `ev-fixture-errors` | Logs-shaped fixture | synthetic, token/email canary redacted at runtime |

Incident Commander, service owner와 Security sign-off: `not_available — portfolio simulation`

