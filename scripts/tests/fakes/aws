#!/usr/bin/env bash

set -euo pipefail

case "$*" in
  *"logs describe-log-groups"*)
    printf '/aws/eks/dev/cluster\t90\tarn:aws:kms:ap-northeast-2:111122223333:key/example\t1024\n'
    printf '/aws/eks/dev/no-expiry\tNone\tNone\t2048\n'
    ;;
  *)
    printf 'unsupported fake aws call: %s\n' "$*" >&2
    exit 64
    ;;
esac
